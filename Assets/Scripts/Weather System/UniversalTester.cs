using UnityEngine;

public class UniversalTester : MonoBehaviour
{
    /*[DisableEdit] public float Toxicity;
    [DisableEdit] public float SingleToxicity;

    private void Awake()
    {
        ToxicityZone.OnImpactRateToxicity += UpdateValue;
        ToxicityZone.OnImpactSingleToxicity += UpdateSecValue;
    }

    // Update is called once per frame
    void Update()
    {

    }

    private void UpdateValue(float value)
    {
        //Debug.Log($"Заражение Rate:<color=green>{value}</color>");
        Toxicity = value;
    }

    private void UpdateSecValue(float value)
    {
        //Debug.Log($"Заражение Single:<color=green>{value}</color>");
        SingleToxicity += value;
    }    

    private void OnDestroy()
    {
        ToxicityZone.OnImpactRateToxicity -= UpdateValue;
        ToxicityZone.OnImpactSingleToxicity -= UpdateValue;
    }*/
}
